<% content_for :title, "Profiles" %>

<h1>Profiles</h1>

<div id="profiles">
  <ul>
    <% @users.each do |user| %>
      <li>
        <%= link_to "#{user.full_name}", profile_path(user) %>
      </li>
    <% end %>
  </ul>
</div>
